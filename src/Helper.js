export const quotesArray = [
    {
		quote:
			`console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log() console.log()`,
		author: 'JavaScript'
	},
	{
		quote:
			` .filter() .map() .filter() .filter() .filter() .filter() .filter() .filter() .filter() .map() .filter() .filter() .map() .filter() .filter() .filter() .filter() .map() .filter() .filter() .map() .filter() .filter() .filter() .filter() .map() .filter() .map() .filter() .filter() .filter() .filter() .filter() .map() .filter() .filter() .map() .filter() .filter() .map() .filter() .filter() .map() .filter() .filter() .filter() .filter() .map() .filter() .filter() .map() .filter() .filter() .filter() .map() .filter() .filter() .filter() `,
		author: 'JavaScript'
	},
	{
		quote:
			"A lot of cheap seats in the arena are filled with people who never venture onto the floor. They just hurl mean-spirited criticisms and put-downs from a safe distance. The problem is, when we stop caring what people think and stop feeling hurt by cruelty, we lose our ability to connect. But when we're defined by what people think, we lose the courage to be vulnerable. Therefore, we need to be selective about the feedback we let into our lives. For me, if you're not in the arena also getting your ass kicked, I'm not interested in your feedback.",
		author: 'Brene Brown'
	},
	{
		quote:
			'Owning our story can be hard but not nearly as difficult as spending our lives running from it. Embracing our vulnerabilities is risky but not nearly as dangerous as giving up on love and belonging and joy-the experiences that make us the most vulnerable. Only when we are brave enough to explore the darkness will we discover the infinite power of our light.',
		author: 'Brene Brown'
	},
	{
		quote:
			`Somehow we've come to equate success with not needing anyone. Many of us are willing to extend a helping hand, but we're very reluctant to reach out for help when we need it ourselves. It's as if we've divided the world into “those who offer help” and “those who need help.” The truth is that we are both.`,
		author: 'Brene Brown'
	}
]

export const random = array => array[Math.floor(Math.random() * array.length)]

export const allowedKeys = [
	'q',
	'w',
	'e',
	'r',
	't',
	'y',
	'u',
	'i',
	'o',
	'p',
	'a',
	's',
	'd',
	'f',
	'g',
	'h',
	'j',
	'k',
	'l',
	'z',
	'x',
	'c',
	'v',
	'b',
	'n',
	'm',
	'Q',
	'W',
	'E',
	'R',
	'T',
	'Y',
	'U',
	'I',
	'O',
	'P',
	'A',
	'S',
	'D',
	'F',
	'G',
	'H',
	'J',
	'K',
	'L',
	'Z',
	'X',
	'C',
	'V',
	'B',
	'N',
	'M',
	';',
	"'",
	',',
	'.',
    '-',
    '{',
    '}',
    '(',
    ')',
    "=",
    '_',
    '+',
    ':',
    '[',
    ']',
    '<',
    '<'
]